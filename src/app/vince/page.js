import React from 'react'

const page = () => {

    // Hey Vince, you can ignore everything else on this page besides the code between return ()
    // anything you code here will be displayed on the website
    
    // 1. inside vscode's terminal type npm install
        // this will download all the modules/packages need to run the website
    // 2. to start the webpage type npm run dev
    // 3. go to --> http://localhost:3000/vince in your browser to see what you've been coding

    // css might not work as intended, to avoid that create elements with specific classes and add styles to your classes inside src/app/globals.css
    // it would be great if you included your css at the way top or bottom...or create a little space for your css using comments

    // guide to forking, pushing, pull requests
    // https://github.com/firstcontributions/first-contributions
    
  return (
    <div>page</div>
  )
}

export default page